#include <stdio.h>
#include <string.h>

int main()
{
    char str[50];
    int i;

    printf("informe uma string: ");
    gets(str);

    for(i=0;i<strlen(str);i++)



printf("\n\n total de espacos brnco %s", str);

}
